# Daily Notes — April 16, 2026

## NexcomAI Progress

### Website Chat — FIXED ✅
- Chat flow was broken (returning "Thanks for reaching out" loop on all inputs)
- Root cause: DB session schema mismatch on Render — `sessions` table required `conversation_id` + `platform` columns we weren't providing
- Fix: Switched to in-memory sessions in `index.js` (bypassed DB for chat sessions entirely)
- Result: Full booking flow works end-to-end on nexcomai.ai
- "Thank you" after booking now returns friendly closing message

### Email Notifications — IN PROGRESS ⚠️
- Tried Gmail SMTP with App Password — Render NOT saving `GMAIL_PASS` env var (shows `gmail_pass_set: false` consistently)
- Gmail required 2-Step Verification setup before App Passwords work
- Hugo's Gmail: nexcomaiai@gmail.com (note: double "ai" — easy to get wrong)
- App password generated: `vango9-miqwec-xorpoR` (may be invalid — generated before 2FA was fully set up)
- Switching to **Resend** (resend.com) — simpler, free, no App Password needed
- Hugo signing up with GitHub on Resend — next step: get API key → add to Render env → update notification.service.js

### Parking Lot Leads — DONE ✅
- Created `/research/PARKING_LOT_LEADS_PINELLAS.md`
- 50+ prospects across 8 tiers: property managers, car dealers, malls, churches, medical, hotels, warehouses, government
- Top priority: Sentry Management (727) 799-8982

### Tomorrow Plan
- Email via Resend (finish today if Hugo gets API key)
- Facebook Messenger integration
- WhatsApp integration
- A2P SMS should be approved (submitted April 14, expect April 16-17)

## Key Decisions
- **Resend over Gmail SMTP**: Gmail app passwords unreliable with Render; Resend is simpler
- **In-memory sessions**: DB schema too complex for chat sessions; in-memory works fine
- **Facebook + WhatsApp**: Deferred to April 17

## Corrections / Lessons
- nexcomaiai@gmail.com has double "ai" — easy typo source
- Render env vars sometimes don't save on edit — must delete + re-add
- Always test env vars with a `/api/test-email` debug endpoint before assuming they're set

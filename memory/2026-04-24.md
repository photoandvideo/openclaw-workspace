# Daily Notes — April 24, 2026

## 🔴 CRITICAL SECURITY ISSUE FIXED

### What Happened
Hugo reported "something not right" after an update. Found critical security issue:
- **Credential files committed to public GitHub:**
  - `.env.txt` (live API keys)
  - `.env2.txt` (duplicate, same keys)
- **Exposed keys:**
  - Minimax API key
  - SendGrid API key
  - Telegram Bot token
  - Twilio SID/token/phone

### What I Did
1. ✅ Removed `.env.txt` and `.env2.txt` from git tracking
2. ✅ Enhanced `.gitignore` to prevent future leaks
3. ✅ Pushed fixes to GitHub
4. ✅ Verified API still running

### Action Required NOW
**Hugo MUST immediately rotate these credentials:**
1. **Minimax** → Delete old key, generate new one, update Render env var
2. **SendGrid** → Revoke old key, generate new one, update Render env var
3. **Telegram** → Delete old bot, create new one via BotFather
4. **Twilio** → Revoke auth token, generate new one, update Render env var

### Root Cause
- Someone (you?) created `.env.txt` and `.env2.txt` as manual backups
- These got accidentally committed before being added to `.gitignore`
- The repo is public, so keys were exposed

### Prevention
- Only use `.env` (which is gitignored)
- Never commit credential backups
- Use Render's built-in "secrets" tab for sensitive data
- Use `git check-ignore -v .env*` before committing

## System Health

✅ **API:** Running and healthy
✅ **Website:** nexcomai.ai live
✅ **Database:** 11 test conversations
⏳ **Resend:** Domain not verified yet (email notifications still blocked)
⏳ **A2P SMS:** Status unknown (10 days no update)

## Git Status

- ✅ BOOTSTRAP.md deleted (cleanup after setup)
- ✅ `.env.txt` and `.env2.txt` removed from git
- ✅ `.gitignore` strengthened
- ✅ All commits pushed to GitHub

---

**Priority:** Rotate credentials immediately. Everything else can wait.
